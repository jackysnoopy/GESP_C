# [GESP202603 四级] 山之谷

## 题目描述
现有一片山地，可以视为一个 $N$ 行 $M$ 列的网格图，第 $i$ 行 $j$ 列的海拔为 $h_{i,j}$。

如果一个单元格的海拔不高于其所有相邻单元格（**相邻包括上、下、左、右、左上、右上、左下、右下，最多 $8$ 个方向**）的海拔，则称该单元格为山谷。

请你数一数该片山地中有多少山谷。

## 输入格式
第一行包含 $2$ 个整数 $N, M$，表示山地的大小。

之后 $N$ 行，每行包含 $M$ 个整数 $h_{i,1}, h_{i,2}, \cdots, h_{i,M}$，表示海拔。

## 输出格式
输出 $1$ 行，包含 $1$ 个整数 $C$，表示山谷的数量。

## 背景
对应的选择、判断题：<https://ti.luogu.com.cn/problemset/1208>

## 样例
### 样例 1
```
3 5
7 6 6 7 9
6 5 6 7 6
6 5 7 8 9
```
```
3
```

## 提示
### 样例解释

样例 1 如图所示，绿色单元格代表山谷：

:::align{center}

![](https://cdn.luogu.com.cn/upload/image_hosting/1xp2gfel.png)

:::

### 数据范围

保证 $1 \leq N, M \leq 100$，$1 \leq h_{i,j} \leq 10^5$。

## 解题思路

### 问题分析
统计网格中所有「山谷」个数：一个格子海拔不高于所有 8 个相邻格子的海拔。

### 核心思路
遍历每个格子，检查 8 个方向的相邻格子是否都满足条件。

### 算法流程
1. 读入 N, M 和海拔矩阵
2. 定义 8 个方向偏移数组
3. 遍历每个格子，检查 8 个邻居
4. 如果所有邻居海拔 ≥ 当前格，计数 +1
5. 输出计数

### 复杂度分析
- 时间复杂度：O(NM)
- 空间复杂度：O(NM)

## 参考代码
```cpp
#include <iostream>
#include <vector>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int N, M;
    cin >> N >> M;
    
    vector<vector<int>> h(N, vector<int>(M));
    for (int i = 0; i < N; i++)
        for (int j = 0; j < M; j++)
            cin >> h[i][j];
    
    int dx[] = {-1, -1, -1, 0, 0, 1, 1, 1};
    int dy[] = {-1, 0, 1, -1, 1, -1, 0, 1};
    
    int ans = 0;
    for (int i = 0; i < N; i++) {
        for (int j = 0; j < M; j++) {
            bool valley = true;
            for (int d = 0; d < 8; d++) {
                int ni = i + dx[d], nj = j + dy[d];
                if (ni >= 0 && ni < N && nj >= 0 && nj < M) {
                    if (h[ni][nj] < h[i][j]) {
                        valley = false;
                        break;
                    }
                }
            }
            if (valley) ans++;
        }
    }
    
    cout << ans << '\n';
    return 0;
}
```
