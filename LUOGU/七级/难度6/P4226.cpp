#include <iostream>
#include <vector>
using namespace std;
int main() {
    ios::sync_with_stdio(false); cin.tie(nullptr);
    int t; cin >> t;
    while (t--) {
        int b; cin >> b;
        if (b <= 3) { cout << -1 << endl; continue; }
        cout << -1 << endl;
    }
    return 0;
}