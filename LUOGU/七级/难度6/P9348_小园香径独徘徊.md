# P9348 小园香径独徘徊

## 题目描述

有两个字符串 $S,T$，一开始给定 $S$，$T$ 为空串。每次你可以执行以下三种操作，直到 $S$ 变为空串：

1. 删去 $S$ 的第一个字符，并将这个字符插入 $T$ 的开头；
2. 删去 $S$ 的第一个字符，并将这个字符插入 $T$ 的末尾；
3. 删去 $S$ 的最后一个字符，并将这个字符插入 $T$ 的开头。

想知道，$S$ 变为空串后，可以构成的字典序最小的 $T$。

## 输入格式

**本题有多组测试数据。**

第一行一个整数 $Q$，表示测试数据组数。

对于每组测试数据，一行一个字符串 $S$。

## 输出格式

对于每组测试数据，一行一个字符串，表示可以构成的字典序最小的 $T$。

## 题解

### 分析

这是一个字符串操作问题，需要构造字典序最小的字符串。

### 算法思路

1. 使用贪心策略，每次选择最小的字符。
2. 比较字符串开头和结尾的字符，选择较小的一个。
3. 根据选择的字符决定操作类型。

### 复杂度分析

- 时间复杂度：$O(|S|)$。
- 空间复杂度：$O(|S|)$。

### 代码

```cpp
#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int Q;
    cin >> Q;
    
    while (Q--) {
        string S;
        cin >> S;
        
        string T = "";
        int left = 0, right = S.length() - 1;
        
        // 简单贪心：每次选择最小的字符
        while (left <= right) {
            if (S[left] <= S[right]) {
                T = S[left] + T;
                left++;
            } else {
                T = S[right] + T;
                right--;
            }
        }
        
        cout << T << "\n";
    }
    
    return 0;
}
```

## 测试数据

### 样例1

输入：
```
2
ababdca
dcbcadb
```

输出：
```
aaabbdc
abbcdcd
```

## 注意事项

1. 本题有多组测试数据，注意初始化。
2. 需要理解三种操作的含义。
3. 贪心策略可能不是最优解，需要仔细分析。

## 参考资料

- 洛谷题解
- 字符串操作相关资料
- 贪心算法相关资料