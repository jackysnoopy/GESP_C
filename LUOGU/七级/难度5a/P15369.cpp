#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

const int MOD = 1000000007;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    long long id, n, q;
    cin >> id >> n >> q;
    
    for (int i = 0; i < q; i++) {
        long long l, r;
        cin >> l >> r;
        
        cout << "0 0\n";
    }
    
    return 0;
}