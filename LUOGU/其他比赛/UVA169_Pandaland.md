# UVA169 - Pandaland（潘达兰）

## 题目描述

给定一个网格图，其中`#`表示陆地，`~`表示水域。判断陆地是否形成一个连通区域（即只有一个岛屿）。

## 输入格式

- 每组：行数R和列数C，然后R行C列的网格
- 以 `0 0` 结束

## 输出格式

`yes`（只有一个连通区域）或 `no`（有多个不连通的区域）

## 算法思路

使用DFS或BFS遍历所有`#`格子，检查连通分量数量是否为1。

**时间复杂度**: O(R × C)
