# UVA175 - Ignatius and the Princess III（伊格内修斯与公主III）

## 题目描述

给定n个数字（1到n），求所有可能的组合数之和。即求2^n。

## 输入格式

多组测试用例。每组一个整数n（1 ≤ n ≤ 30）。

## 输出格式

2^n的值

## 算法思路

直接计算2的n次幂。可以使用位运算 `(1 << n)` 或循环计算。

**时间复杂度**: O(n) per test case
