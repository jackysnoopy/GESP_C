# UVA174 - Paint（绘画）

## 题目描述

给定一个邻接矩阵，判断其是否为有效的无向图邻接矩阵（对称且对角线为0）。

## 输入格式

多组测试用例。每组：整数N（节点数），然后N×N的邻接矩阵。

## 输出格式

`true`（有效）或 `false`（无效）

## 算法思路

检查邻接矩阵是否满足：
1. 对角线元素全为0
2. 矩阵关于主对角线对称（a[i][j] == a[j][i]）

**时间复杂度**: O(N²)
