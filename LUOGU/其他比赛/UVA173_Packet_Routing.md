# UVA173 - Packet Routing（数据包路由）

## 题目描述

给定一个网络，包含N个节点和M条边（每条边有权重），求从节点1到节点N的最短路径。

## 输入格式

多组测试用例。每组：节点数N和边数M，然后M行每行三个整数u v w（表示u到v有一条权重为w的边）。

## 输出格式

从节点1到节点N的最短路径长度

## 算法思路

使用Dijkstra最短路算法，从节点1出发，求到节点N的最短距离。

- 使用优先队列（小顶堆）优化Dijkstra
- 无向图，建双向边

**时间复杂度**: O((N + M) log N)
