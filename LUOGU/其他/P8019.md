# P8019 [ONTAK2015] OR-XOR

## 解题思路

贪心从高位到低位尝试。对于每个二进制位，判断是否能让所有段的异或和在该位为0。

具体做法：从最高位（60位）开始，对于当前位 `bit`，维护一个掩码 `mask = ans | ((1<<bit) - 1)`，表示之前已确定的高位加上当前位以下所有位。遍历数组，计算前缀异或，如果前缀异或与 `mask` 的与为0，则可以在该处分段。统计分段数，如果分段数 ≥ m 且最后的前缀异或也满足条件，则该位可以为0（即 `ans` 加上该位）。

时间复杂度 O(60n)。
