#include <iostream>
#include <vector>
using namespace std;

// [HGOI-1] Build
// Without data file.
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    cout << n << "\n";
    return 0;
}
